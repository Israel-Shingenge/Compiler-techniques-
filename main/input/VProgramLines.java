package main.input;

import java.util.List;

public class VProgramLines {
    public static final List<String> lines = List.of(
        "BEGIN",
        "INTEGER A, B, C, E, M, N, G, H, I, a, c",
        "INPUT A, B, C",
        "LET B = A */ M",
        "LET G = a + c",
        "temp = <s%**h - j / w +d +*$&",
        "M = A/B+C",
        "N = G/H-I+a*B/c",
        "WRITE M",
        "WRITEE F"
    );
}
